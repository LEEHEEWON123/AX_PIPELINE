# AX 파이프라인 — 개발 흐름 정의

스펙 기반 개발(SDD). 이슈 1건이 개발항목 1건이고, 그 번호가 spec·plan·검증·문서·PR까지 그대로 이어진다.

- **이 리포의 역할**: 파이프라인 전용. 커맨드·템플릿·레지스트리·실행상태만 둔다.
- **제품 코드와 spec**: 각 제품 리포의 이슈 브랜치에 함께 커밋한다.

## 추적 축

하나의 번호가 전 단계를 관통한다.

```
Total PRD 개발항목번호  →  GitHub 이슈  →  브랜치  →  spec.md
    →  plan.md  →  verify.md  →  문서 섹션 태그  →  PR 제목
```

문서(7번)에 붙는 `선택-6` 같은 태그 칩이 곧 Total PRD 항목번호다. 문서만 보고도 어느 스펙에서 나온 화면인지 역추적된다.

## 리포 배치

**AX_PIPELINE (이 리포)**

```
PIPELINE.md              이 문서
projects.yml             대상 프로젝트 레지스트리
templates/               total-prd / spec / plan / verify / doc 템플릿
.claude/commands/        ax-* 슬래시 커맨드
.claude/skills/          문서 규격 스킬
.ax/state/               프로젝트별 이슈 진행 상태 (gitignore)
workspace/               대상 리포 클론 위치 (gitignore)
```

**제품 리포 (이슈 브랜치)**

```
specs/
  total-prd.md                   기본 브랜치에 유지. 개발항목 번호의 원장
  items/<이슈번호>/
    spec.md                      개발항목 1건의 스펙
    plan.md                      심층분석 기반 구현 계획
    verify.md                    검증 결과 리포트
    docs/index.html              사용자 안내서
    docs/shots/*.png             6번에서 캡처한 화면
```

## 단계

### 0. 프로젝트 등록 — `projects.yml`

프로젝트당 한 번. 리포 주소, 기본 브랜치, FE/BE 라벨 규칙, 로컬 워크스페이스 경로, 그리고 **앱 기동 방법과 접속 URL**을 적는다. 마지막 항목이 6번 화면 검증의 전제다.

### 1. 이슈 수집 — `/ax-sync <project>`

`gh issue list --repo <repo> --state open --json number,title,labels,body,assignees,milestone`

- 리포로 FE / BE 분류 — 레지스트리 `repos.fe` / `repos.be` 로 판별한다. 라벨이 비어 있는 팀이 많아 라벨에 기대지 않는다
- 같은 기능의 FE·BE 짝 이슈를 찾아 연결
- 결과를 `.ax/state/<project>/backlog.json`에 저장하고 표로 출력

**게이트** — 이슈 본문에 수용기준이 없으면 표시해 둔다. 3번에서 반드시 보완한다.

### 2. 브랜치 — `/ax-branch <issue>`

`feat/fe/<번호>-<slug>` 또는 `feat/be/<번호>-<slug>`. 기본 브랜치 최신에서 딴다.
BE 선행 의존이 걸린 FE 이슈면 경고를 띄운다.

### 3. PRD — 2단 구조

**3a. `/ax-prd <project>`** — Total PRD를 기본 브랜치에 만든다. 개발항목 번호는 이미 이슈 제목에 대괄호 형식으로 박혀 있으므로 새로 채번하지 않고 그대로 수입한다 (`공통-N` = 화면 없이 데이터를 쌓는 항목, `선택-N` = 화면으로 드러나는 항목).

**3b. `/ax-spec <issue>`** — 이슈별 `spec.md`. Total PRD 항목번호를 반드시 참조한다.

필수 섹션:

| 섹션 | 내용 |
|---|---|
| 개발항목 | Total PRD 번호 |
| 목적 / 대상 사용자 | 누가 무엇을 위해 쓰는가 |
| 범위 · 비범위 | 이번 이슈에서 하지 않는 것을 명시 |
| 수용기준 | Given-When-Then, 항목마다 ID 부여 |
| 화면 · API 계약 | FE는 화면 상태, BE는 요청/응답 스키마 |
| 의존 | `depends-on: #<번호>` |
| 문서 반영 포인트 | 7번에서 캡처할 화면과 설명할 지점 |

**수용기준은 6번 검증의 입력이다.** 확인 가능한 문장으로 쓴다. "사용성이 좋아야 한다"는 검증할 수 없고, "종료 버튼을 누르면 유형·사유 선택 창이 뜬다"는 검증할 수 있다.

**게이트** — 사람 승인.

### 4. Plan — `/ax-plan <issue>`

심층분석. 기존 코드 탐색, 영향 범위, 데이터 모델 변경, 마이그레이션 필요 여부, 엣지케이스를 조사한다. 코드베이스 조사는 병렬 서브에이전트로 나눠 돌린다.

산출: `plan.md` — 단계별 작업 순서, 건드릴 파일 목록, 리스크, 롤백 방법.

**게이트** — 사람 승인. SDD의 핵심 관문이다. 여기를 통과하기 전에는 코드를 쓰지 않는다.

### 5. 구현 — `/ax-build <issue>`

`plan.md` 단계 순서대로 진행하고, 완료할 때마다 체크박스를 갱신한다. 중단해도 다음 세션에서 이어서 재개된다.

### 6. 검증 — `/ax-verify <issue>`

네 갈래로 나뉜다.

**6-A 기계 검증** — 빌드 · 테스트 · 린트.

**6-B spec 대조** — `claude -p` 헤드리스로 서브에이전트를 띄워 `spec.md`의 수용기준 각 항목을 실제 diff와 대조시킨다. 항목별로 `PASS` / `FAIL` / `UNVERIFIABLE` 판정과 근거를 받는다.

**6-C 화면 검증 (FE 이슈)** — Chrome 확장으로 앱을 직접 조작한다. 수용기준 시나리오를 순서대로 실행하고, **확인 지점마다 스크린샷을 `docs/shots/`에 저장한다.** 이 캡처가 7번 문서의 자료가 된다.

**6-D API 계약 검증 (BE 이슈)** — 화면이 없으므로 6-C 대신 실제 요청을 쏘고 응답을 spec의 스키마와 대조한다.

산출: `verify.md` — 수용기준 ID별 판정 + 근거 + 스크린샷 경로.

**게이트** — `FAIL`이 하나라도 있으면 5번으로 되돌린다. `UNVERIFIABLE`은 사람이 판단한다.

> **FE·BE 짝 이슈 주의**
> 같은 기능을 FE·BE로 쪼개면 BE가 머지되기 전에는 FE 화면이 동작하지 않아 6-C를 단독으로 통과할 수 없다. 두 가지 중 하나를 택한다.
> - 로컬에서 짝 BE 브랜치를 함께 체크아웃해 통합 상태로 검증한다 (권장)
> - 6-A·6-B만 먼저 통과시키고 6-C는 BE 머지 후로 보류한다

### 7. 문서화 — `/ax-doc <issue>`

**개발자 문서가 아니라 현업 사용자용 안내서다.** 참고 규격은 CRM 성장·유지 안내서를 따른다.

- 6-C에서 찍은 스크린샷을 그대로 쓰고, 설명할 지점에 번호 핀을 얹는다
- 핀 번호와 1:1로 대응하는 legend 목록을 붙인다
- 섹션 제목 옆에 개발항목 태그 칩을 단다
- 개발항목 ↔ 확인 위치 추적표를 넣는다
- 주의·경고는 콜아웃 박스로 분리한다
- 라이트/다크 양쪽에서 읽히게 하고, 인쇄 스타일을 넣는다

Artifact로 게시해 팀 공유 링크를 만든다.

### 8. PR — `/ax-pr <issue>`

브랜치 1개 = 이슈 1개 = PR 1개. 2번에서 이미 이슈 단위로 갈라졌으므로 여기서 더 쪼갤 것은 없다.

```
gh pr create --repo <repo> --base <default> \
  --title "[<개발항목번호>] <제목>" --body <본문>
```

본문 구성: spec 요약 · 수용기준 체크리스트 · verify 판정 결과 · 문서 링크 · 짝 이슈 PR 링크 · `Closes #<번호>`

## 상태 관리

`.ax/state/<project>/<이슈번호>.json`에 현재 단계와 통과한 게이트, 타임스탬프를 남긴다. 어느 단계에서 끊겨도 다음 세션이 이어받는다.

## 게이트 요약

| 관문 | 위치 | 통과 조건 |
|---|---|---|
| 스펙 확정 | 3 이후 | 수용기준이 검증 가능한 문장인가 |
| 계획 승인 | 4 이후 | 사람이 plan.md를 승인 — 코드 착수 전 필수 |
| 검증 통과 | 6 이후 | FAIL 0건 |
| 문서 완료 | 7 이후 | 모든 개발항목이 추적표에 나타나는가 |

## 이슈 본문 규약

파이프라인은 이슈 본문에서 세 가지를 읽는다. 필드명은 `projects.yml`의 `issue`에서 바꾼다.

| 읽는 것 | 기본 필드 | 쓰이는 곳 |
|---|---|---|
| 개발항목 번호 | 제목의 `item_pattern` | 추적 축 전체 |
| 선행 | `**선행**` | 2번 의존 점검, 착수 순서 |
| 완료 판정 | `**완료 판정**` | 3b 수용기준의 씨앗 |

### 개발항목 번호는 채번하지 않는다

팀이 이미 이슈 제목에 번호를 달아 쓰고 있으면 그것을 그대로 수입한다. 파이프라인이 새 번호를 매기면 기존 문서·메일과 어긋난다. `item_pattern`이 `null`이면 번호 추적 없이 이슈번호만으로 돈다.

### 선행 표기는 흔들린다

사람이 손으로 쓰는 필드라 한 리포 안에서도 표기가 섞인다. 파서는 아래를 전부 받아야 한다.

- `공통-1, 공통-2` — 하이픈
- `공통-1·2·4, 선택 1·4·5` — 가운뎃점 압축. **접두어 뒤가 하이픈이 아니라 공백인 경우가 있다**
- `other_repo#11` — 타 리포 이슈 참조
- `없음`

압축 표기는 앞 접두어를 뒤 숫자에 전부 배분해 푼다. 단순 매칭으로 짜면 절반을 조용히 놓친다.

### 완료 판정은 그대로 쓸 수 없다

대개 한 줄이다. 사람은 알아보지만 6-B 기계 대조는 판정하지 못한다. 품질이 갈리는 지점:

- **쓸 만함** — "월 중 담당 변경 계정이 개인·팀별 각각 반영되면서 회사 고유 수는 1건으로 집계". 거의 그대로 AC가 된다
- **모호함** — "계정 1개의 **전체** 이력이 화면에서 확인됨". '전체'가 무엇인지 미정이라 판정 불가
- **외부 의존** — "**검증 쿼리**와 일치". 그 쿼리가 어디 있는지 spec에 적어야 6-D가 돈다

3b에서 Given-When-Then으로 펼치고 항목마다 ID를 붙인다.

## FE/BE 범위 중복을 먼저 자른다

FE·BE가 리포로 갈린 팀에서 흔한 함정이다. BE 이슈 본문에 "프론트 화면 작업 포함"이라 적혀 있는데 같은 화면의 FE 이슈가 따로 있는 경우가 있다.

이대로 두면 5번에서 **중복 구현되거나, 서로 상대가 할 거라 믿고 둘 다 안 한다.**

3b에서 BE spec의 비범위에 "화면 작업 제외 — fe#N 소관"을 못 박는다. 이슈 본문 자체를 고치는 게 더 낫지만, 그건 외부에 반영되는 변경이라 사람이 판단한다.

## 화면 없는 항목

FE 짝이 없는 항목은 단독 화면 없이 다른 화면의 값으로만 나타난다. 6-C(화면 검증)를 건너뛰고 6-D(API 계약 검증)로 판정하며, 7번 문서에서는 추적표의 "확인 위치"로만 등장한다.

`/ax-prd`가 Total PRD를 만들 때 항목별 화면 유무를 판정해 적어두면 6번과 7번이 그 값을 따른다.

## 기존 관례를 이긴다고 생각하지 않는다

파이프라인을 붙이는 리포에는 대개 이미 관례가 있다.

- **기본 브랜치** — `main`이라고 가정하지 않는다. `master`인 리포가 흔하다
- **브랜치 명명** — 팀이 쓰던 규칙이 있으면 `branch_pattern`에 그것을 적는다. 파이프라인이 새 규칙을 들고 들어가면 기존 작업과 갈라진다
- **브랜치 구조** — 서로 쌓여 있는(stacked) 구조면 base가 기본 브랜치가 아니다
- **CI** — 이미 PR 게이트가 있으면 6-A는 그것을 부르고, 새로 만들지 않는다

`/ax-branch`가 브랜치를 만들기 전에 `git branch -r`부터 훑는 이유다.

## 프로젝트 보드 — 나중에 붙임

GitHub Projects 보드를 쓰는 팀이어도 지금은 연동하지 않는다. 보드가 주는 통합 뷰는 연동 없이도 이미 얻고 있고, 연동으로 추가되는 건 **카드 상태 자동 이동** 하나뿐이다. 붙이는 비용이 나중에도 동일하게 싸므로 미룬다.

단계 전이는 `.ax/state/<project>/<이슈번호>.json`에 기록되므로, 나중에 각 커맨드 끝에 `gh project item-edit` 한 줄만 더하면 된다. 그때 `gh auth refresh -s project`로 쓰기 스코프를 추가한다 — 기본 토큰은 `read:project`까지만 있는 경우가 많다.

**진행 상태의 진실은 보드가 아니라 `.ax/state`다.** 보드는 사람이 손으로 옮기는 동안 거짓말을 할 수 있다.
